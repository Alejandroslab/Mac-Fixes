In order to create a spacer in the dock

defaults write com.apple.dock persistent-apps -array-add '{tile-data={}; tile-type="spacer-tile";}'

Then:

killall Dock

And you are done.



You can also use tile-type="small-spacer-tile";

defaults write com.apple.dock persistent-apps -array-add '{tile-data={}; tile-type="small-spacer-tile";}'

killall Dock


REMEMBER that the command line is case sensitive therefore 'Dock' is not the same as 'dock'
